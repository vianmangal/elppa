PORT=5001
GITHUB_TOKEN=check env
OPENROUTER_API_KEY=check env
OPENROUTER_MODEL=openrouter/free
# Set to false to return an error instead of a repository-derived overview when OpenRouter is unavailable.
ENABLE_LOCAL_ANALYSIS_FALLBACK=true
